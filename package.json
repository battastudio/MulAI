{
  "name": "multi-ai-desktop",
  "version": "1.1.0",
  "description": "Every AI chat — Claude, ChatGPT, Gemini, DeepSeek, Qwen and more — in one window. Type once, send to all.",
  "license": "MIT",
  "author": "Hassan Al-Najjar",
  "type": "commonjs",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "lint": "node scripts/check-lines.js",
    "test": "node cookies/common.js --selftest && node cookies/macos.js --selftest && node cookies/linux.js --selftest && node cookies/windows.js --selftest && node cookies/index.js --selftest",
    "package:mac": "electron-packager . Multi-AI --platform=darwin --arch=arm64,x64 --icon=build/icon.icns --out=dist --overwrite --app-bundle-id=com.nour.multiai --ignore=dist --ignore=\\.git",
    "package:win": "electron-packager . Multi-AI --platform=win32 --arch=x64 --icon=build/icon.ico --out=dist --overwrite --ignore=dist --ignore=\\.git",
    "package:linux": "electron-packager . Multi-AI --platform=linux --arch=x64 --icon=build/icon.png --out=dist --overwrite --ignore=dist --ignore=\\.git",
    "package": "npm run package:mac"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.1",
    "electron": "^33.2.0"
  }
}
